import 'package:flutter/material.dart';

class Palette {
  static const background = Color(0xFFFAFAF8); // page background
  static const surface = Color(0xFFFFFFFF); // cards, list rows
  static const ink = Color(0xFF1C1C1A); // primary text
  static const muted = Color(0xFF8A8A85); // secondary text, labels
  static const accent = Color(0xFF2F5233); // buttons, highlights, positive
  static const hairline = Color(0xFFE8E7E2); // dividers, borders
}
